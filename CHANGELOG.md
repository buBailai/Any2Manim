# 更新日志 Changelog

Any2Manim 所有面向用户的变更都记录在此文件。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)：
**主版本号** 不兼容（数据库 schema 不向下兼容）·
**次版本号** 加功能（向下兼容）·
**修订号** 仅修 bug。

> 页面左上角的版本号与右上角「更新日志」浮窗都直接读取本文件，
> 以后发版只需更新这里，无需改页面代码。

---

## [1.0.4] - 2026-06-21

> 修 1.0.3 教学升级带来的成功率回退，并让免安装包内置轻量 LaTeX，公式开箱即用。

### Fixed
- **成功率回退（重点修复）**：1.0.3 倾向大量生成公式动画（MathTex），而 MathTex 必须有 LaTeX——免安装包默认不带 → 一遇公式就报「缺依赖」失败；公式代码本身也较脆，自愈常修不好、白等很久。现改为 **LaTeX 自适应**：检测不到 LaTeX 时自动改用 Text（Unicode 上下标）写公式，永不因缺依赖失败；检测到 LaTeX 则用精简模板渲染真公式。同时代码生成「稳优先」——保留教学结构，但避开易翻车写法，渲染更快更稳。
- **失败也保留代码、可继续改**：自愈多次仍未成功时，这次尝试的代码会完整保留——「代码」tab 能看到、时间线里的失败版可点开，直接在左侧说「把 X 改成 Y」即在这次基础上继续调整，不必从头重来。（修了一处会把失败代码清空成空白的 bug。）
- 生成过程中代码会**提前显示**在「代码」tab，等待时看得到产出。

### Added
- **免安装包内置轻量 LaTeX（TinyTeX）**：公式排版开箱即用，大多数没装 LaTeX 的老师电脑也能正常渲染数学 / 物理公式。
- **`安装LaTeX.bat`（可选升级）**：随包附带，老师可按需选「最小 / 标准 / 完整」三档自行扩充 LaTeX 宏包，获得更全的符号 / 公式支持；不运行也不影响内置的基础公式能力。

---

## [1.0.3] - 2026-06-21

> 教学质量大升级（借鉴 [Math-To-Manim](https://github.com/HarleyCoops/Math-To-Manim) 的「先教学后符号 / 镜头即叙事 / 公式即角色」理念），并新增解说停顿与免导出试听。

### 教学质量（生成的视频更像「在讲课」而非「念字幕」）
- **结构化教学规划**：分镜从自由文本升级为「一次产出的结构化教学镜头计划」——先建立直觉、再引入符号；每个镜头都有明确「教什么」；开场有大标题、结尾有结论。代码生成改为「忠实翻译这份计划」，不再自由发挥。
- **镜头动作语法**：引入 HEADLINE / SHOW / CAPTION / FOCUS / REVEAL_FORMULA / TERM_TOUR / COMPARE / TAKEAWAY 等受控动作词，让画面承担教学叙事。
- **公式即角色**：数学 / 物理公式自动拆成各项，逐项讲解、稳定着色（仅在涉及公式时触发，不影响历史 / 语文等学科）。
- **教学积木组件库**：新增 `a2m_headline / a2m_safe_caption / a2m_takeaway / a2m_formula_with_caption / a2m_term_tour / a2m_compare_layout / a2m_vt_graph / a2m_number_line / a2m_timeline`，布局更稳、更少重叠。
- **「能教」质检**：渲染通过后再做一遍静态体检（是否只是标题卡 / 是否缺结论 / 是否贴字幕区 / 节奏是否过快），发现「只有文字没有画面」会自动补救成真正的动画。
- **自愈不再塌缩**：修复渲染报错时带上完整教学计划，不会为了跑通就把多镜头的片子简化成一个图形。

### 配音 / 字幕 Added
- **解说停顿**：在解说稿里插入 `[停顿1.0]` 标记（秒数可改，1 位小数），配音会在该处停顿对应秒数、字幕同步留空（音频层插静音实现，精确可控）。
- **免导出试听**：解说稿可一键合成预览音频，在预览区跟随视频同步播放，边看边听校对节奏，不必每次导出整片。临时预览音频会在改稿 / 导出后自动清理，不堆积。

### Changed
- 烧录字幕位置整体下移（让出与动画自带文字的间距），随分辨率等比缩放。

---

## [1.0.2] - 2026-06-21

> 继续修 Windows 免安装包首测发现的配音 / 字幕问题。

### Fixed
- **配音 / 字幕静默失败**：之前 edge-tts 合成或 ffmpeg 合音轨失败时不报错，直接导出一个没声没字幕的视频。现在每步失败都会在控制台打日志，并在页面提示「配音/字幕未成功」及原因。
- **edge-tts 配音走代理**：edge-tts 默认不读系统代理，大陆网络直连微软语音端点常报 `403`。现在会自动读取系统代理（如 Clash 开启的系统代理），也可用环境变量 `A2M_TTS_PROXY` 显式指定。
- **edge-tts `403` 提示时间问题**：edge-tts 的鉴权 token 按系统时间计算，**系统时间不准会 100% 报 403**——日志/文档已提示先校准系统时间。

### 打包（Windows 免安装包）
- ffmpeg 探测增加诊断日志：找不到可执行文件、缺 libass 等都会在控制台说明。
- 启动脚本预留 `A2M_TTS_PROXY` 可编辑行；附「更新配音组件（edge-tts）.bat」便于升级。

---

## [1.0.1] - 2026-06-21

> 修复首次在 Windows 上测试发现的兼容问题，使免安装包能正常渲染。

### Fixed
- **Windows 渲染报错 `[WinError 2] 系统找不到指定的文件`**：渲染引擎改用 `python -m manim` 调用，不再按路径找 `manim` 可执行文件（Windows 内嵌 Python 无 `bin/manim`，原方式找不到）。Mac / Windows / Linux 统一。

### 打包（Windows 免安装包）
- 安装脚本补装 `setuptools` / `wheel`：manim 的依赖 `srt` 仅有源码包、需现场构建，缺这两个会报 `Cannot import 'setuptools.build_meta'`。
- 启动 / 安装脚本内容改为纯 ASCII（中文只留在说明 .txt）：避免中文 Windows 下 `.bat` 里的中文被按 GBK 误解析成命令。

---

## [1.0.0] - 2026-06-20

> 首个公开版本。把「老师用一句话描述 → AI 生成教学动画视频」的完整链路跑通：
> 描述 → 分镜 → 生成 Manim 代码 → 自愈渲染 → 对话式修改 → 预览 → 配音字幕 → 导出。
> 仿 OpenMentor 的部署哲学：开源、自带 Key（BYO-Key）、数据本地自主、一键启动。

### 核心能力 Added
- **自然语言生成教学动画**：输入一句教学需求，AI 先拆分镜、再写 ManimCE 代码、自动渲染成视频。
- **自愈渲染循环**：代码渲染报错时把 traceback 喂回模型自动修复（AST 静态校验 → 试渲 → 错误分类 → 定向修），有界重试、会喊停、保住最后一个可渲版本，不把报错甩给老师。
- **对话式定向编辑**：「把三角形改成蓝色」这类修改只产生最小代码改动（search/replace），匹配不上才整段重生成。
- **好默认观感**：内置靛蓝深色画布 + 统一色板/字体/留白 + 组件库 helper（标题/注释/高亮/坐标系），出片即好看。
- **演示模式**：未填 API Key 时用内置范例离线出片（勾股/函数/圆等），先看效果再决定是否接入模型。

### 模型接入 Added
- **自带 Key（BYO-Key）多厂商**：内置 DeepSeek、豆包·火山方舟、通义千问、智谱 GLM、硅基流动、Cherry Studio 企业版、Kimi、OpenAI、Ollama（无需 Key）、自定义（OpenAI 兼容）等预设，可一键测试连通。

### 导出与配音 Added
- **多格式导出**：高清视频 MP4（720p/1080p/4K）、轻量 GIF（嵌课件）、封面图 PNG。
- **封面任意选帧**：拖动预览进度条到想要的一帧再导出封面，不再是默认黑屏首帧。
- **AI 配音 + 字幕**：edge-tts 多音色 / 可调语速；AI 按分镜时长自动写旁白（控制字数避免配音超时），可在「解说字幕」里手动编辑；字幕支持烧录进画面或外挂 SRT。
- **导出文件按项目名命名**：如 `勾股定理_v1.mp4`、`勾股定理_v1_封面.png`。

### 素材与示例 Added
- **素材上传**：上传图片 / SVG 进动画，AI 可在画面里引用。
- **分学科示例库**：覆盖数学、物理、化学、信息、历史等多学科的现成示例，点一下填入即可。

### 工作区 Added
- **左对话 / 右画布**：右侧含预览、代码、渲染状态、解说字幕四个页签，底部版本时间线可回溯历史版本（靠代码重渲，不囤视频）。
- **播放器式进度条**：悬停出现拖拽手柄，可拖动选帧 / 点击任意位置跳转（鼠标、触摸均支持）。
- **项目归档**：项目可归档（只读）与恢复，列表支持搜索与分类。
- **局域网访问**：`./start.sh` 绑定 `0.0.0.0`，同网段设备可直接访问。

### 界面 Added
- **深色 / 浅色模式**：右上角一键切换，本地记忆，首屏前生效不闪屏。
- **简体 / 繁体切换**：右上角一键切换，OpenCC 词组级转换（台湾用词），纯前端只转界面文字、不影响内容与逻辑。
- **更新日志浮窗 + 动态版本号**：左上角版本号与本浮窗都读取 CHANGELOG，发版只改文档。

### 技术架构
- 后端 FastAPI + SQLite + asyncio 单 worker 渲染队列；渲染引擎 ManimCE 0.20.1（社区版，非 ManimGL）。
- 前端纯 HTML/CSS/JS，无构建步骤；SSE 实时推送生成 / 渲染 / 导出进度。
- 视频用 `static-ffmpeg`（自带 libass，字幕烧录开箱即用）。
- 存储哲学：代码廉价、视频昂贵 —— 长期只存代码 + 元数据 + 缩略图，预览视频可按需清理。

### 已知限制
- 渲染在本地 CPU，复杂场景较慢；含 LaTeX 公式需本机有 TeX 环境（打包版的依赖分发在后续版本完善）。
- 个人版单 worker 串行渲染；多用户 / 校园版（多 worker、沙箱、配额）尚未包含。
